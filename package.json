{
  "name": "cosmo",
  "version": "0.0.1",
  "private": true,
  "description": "Cosmo — the demo agent for AGENT. Pays SOL for the API calls it needs.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "fund": "node src/scripts/fund.js",
    "health": "curl -s http://localhost:4019/api/health"
  },
  "dependencies": {
    "@solana/web3.js": "^1.95.4",
    "bs58": "^5.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
